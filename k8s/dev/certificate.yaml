apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-sairen-lt
spec:
  secretName: wildcard-sairen-lt-tls
  dnsNames:
    - "*.sairen.lt"
    - sairen.lt
  issuerRef:
    name: letsencrypt-cloudflare
    kind: ClusterIssuer
